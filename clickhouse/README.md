# Clickhouse

-   [ch playground](https://sql.clickhouse.com/)
-   читает только те столбцы, которые используются в запросе (в отличие от строчно-ориентированной, которая читает всю строку (наименьший фрагмент данных по архетиктуре))
-   приблизительный расчет, вместо точного, что дает большую производительность
-   Колонночные хранилища особенно хорошо подходят для такого сжатия, поскольку значения одного типа и с одинаковым распределением данных располагаются рядом.
-   Сжатие данных не только уменьшает объём данных в таблицах базы данных, но во многих случаях также улучшает производительность запросов, поскольку локальные диски и сетевой ввод-вывод часто ограничены низкой пропускной способностью.
-   рекомендуемая вставка в таблицы десятки, сотни тысяч за раз для минимизации создания part-ов
-   [Useful question-answer](./FAQ.md)
